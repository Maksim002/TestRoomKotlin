<resources>

    <!-- Base application theme. -->
    <style name="AppThemeMi" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/color_text_w_pink</item>
        <item name="colorAccent">@color/color_text_w_pink</item>
    </style>

    <style name="AppThemeAll" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/color_text_w</item>
        <item name="colorAccent">@color/color_text_w</item>
    </style>
</resources>
